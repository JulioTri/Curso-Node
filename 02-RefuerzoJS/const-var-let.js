let nombre = 'Wolverine';

if (true) {
    /* let nombre = 'Magneto'; */
    nombre = 'Magneto'
    
    console.log(nombre)
}

/* const indica que no se cambiara el contenidod e la variable, mientras que let si lo hace, ademas, var ya no se usa
debido a que trae algunos problemas como que si se define despues de llamarse la variable seguira siendo valida, tambien
permite definir varias veces la misma variable y sebre escribirla lo que podria generar un problema */

console.log(nombre)